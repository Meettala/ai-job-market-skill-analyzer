# Deterministic rule-based extraction is the default. A provider key alone does
# not enable external calls. To opt in, set ENABLE_PROVIDER_MODE=true and one key.
ENABLE_PROVIDER_MODE=false
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
